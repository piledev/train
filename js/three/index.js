const main = () => {
  const target = [];
};

// 8この配列を配列に格納して2次元配列を作る
const getEightLines = () => {
  const eightLines;
  return eightLines;
};

const is_three = mark => {
  return true;
};

// 以下は対戦相手の実装アイデア -------------------------------
const is_two = (mark, line) => {
  const indexOfSpace;
  return true, indexOfSpace;
};
